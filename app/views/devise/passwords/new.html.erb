<section>
	<div class="container text-center col-lg-5 col-sm-8">
		<h2>Mot de passe oublié</h2>
		<h3 class="text-danger"> ATTTENTION, pour l'instant l'appli n'envoit pas de mail pour changer le mot de passe, je fais ça la semaine pro, j'étais pressée de vous le montrer, mais pas de souci ! Contactez moi sur la page des psychodingues ou par mail et je changerais votre mot de passe manuellement. Vous en faites pas c'est temporaire ! </h3>

		<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
		  <%= render "devise/shared/error_messages", resource: resource %>

		  <div class="input-group mb-4 mt-4">
		      <div class="input-group-prepend">
		        <span class="input-group-text"><i class="fas fa-envelope-open-text"></i></span>
		      </div>
			    <%= f.email_field :email, autofocus: true, placeholder:raw('Adresse email'), autocomplete: "email", class: "form-control" %>
			  </div>

			  <div class="actions">
			    <%= f.submit "Réinitialiser le mot de passe", class: "btn btn-primary mb-3" %>
			  </div>
			<% end %>

		<%= render "devise/shared/links" %>
	</div>
</section>
